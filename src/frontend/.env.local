# URL van je FastAPI backend (zonder trailing slash)
API_BASE=http://localhost:8000
# Public base URL van de Next app (voor server-side fetch in SSR, optional)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
